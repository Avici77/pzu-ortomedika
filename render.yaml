services:
  - type: web
    name: pzu-ortomedika-booking
    env: node
    rootDir: booking-system
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_PATH
        value: /var/data/data.db
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
    disk:
      name: booking-data
      mountPath: /var/data
      sizeGB: 1
